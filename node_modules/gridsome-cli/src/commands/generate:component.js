module.exports = {
  name: 'generate:component',
  description: 'Create new component inside sr/components',
  run: async toolbox => {
    const {
      parameters,
      createComponent,
    } = toolbox;

    const name = parameters.first;

    await createComponent('src/components', 'component', name)
  },
};
