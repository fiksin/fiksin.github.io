module.exports = {
  name: 'generate:layout',
  description: 'Create new layout inside src/layouts',
  run: async toolbox => {
    const {
      parameters,
      createComponent
    } = toolbox

    const name = parameters.first;

    await createComponent('src/layouts', 'layout', name)
  }
}
